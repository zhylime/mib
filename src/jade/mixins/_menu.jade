mixin menu(n)
  .c-menu.l-columns
    each item, i in main_menu
      - var _klass = i == n ? 'active' : '';
      .c-menu--item.col.l-w--20p(class = _klass)
        .c-menu--item--icon.ic--menu(class=item.icon)
        span.c-menu--item--name!=item.name

